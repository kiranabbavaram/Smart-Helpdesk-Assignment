# Client environment

# In development, the client uses a Vite proxy so API requests to 
# "/api" are forwarded to http://localhost:8080 automatically.

# For production builds (e.g., Docker), set VITE_API_URL to your API base URL.
VITE_API_URL=http://localhost:8080/api


